node_modules
lib/icons
.wrangler
.dev.vars
.env
.env.local
dist
*.log
.DS_Store
.claude/
# Local-only assets uploaded to R2 (R2 is the source of truth, no need to commit binaries).
assets/
# .dev.vars holds actual secrets (tokens, keys) — keep ignored.
# wrangler.jsonc only has resource names/IDs (not credentials) so it's committed.
